apiVersion: apps/v1
kind: Deployment
metadata:
  name: diplom-app
  namespace: diplom
  labels:
    app: web-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: diplom-test-site
        image: seodwaekki/html-image
        resources:
          requests:
            cpu: "0.5"
            memory: "200Mi"
          limits:
            cpu: "1"
            memory: "400Mi"
        ports:
        - containerPort: 80

